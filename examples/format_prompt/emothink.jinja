{{ content | trim }}

You should FIRST think through the coarse reasoning process and provide the coarse answer as an internal monologue.
THEN think through the fine-grained reasoning process as an internal monologue, and finally provide the answer to the question.

The output format must be:

<coarse_thought> ...your coarse reasoning here... </coarse_thought>
<coarse_answer>positive|negative</coarse_answer>
<fine_thought> ...your fine-grained reasoning here... </fine_thought>
<final_answer>the answer to the question</final_answer>